#Calcule du barreaudage

## Initialisation du dépôt

```bash
git init
git remote add origin SSH_REPO
git add Fichier.XXX
ou 
git add . => pour tous les fichiers
git commit -m "Version n°"
git status => pour vérifier
```
